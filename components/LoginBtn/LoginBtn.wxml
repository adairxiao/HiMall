<!-- components/LoginBtn/LoginBtn.wxml -->
<view class="container" wx:if="{{isLogin}}" bindtap="handleTap">
  <slot></slot>
</view>
<view class="container" wx:else>
  <slot></slot>
  <button class='auth' open-type="getUserInfo" bindgetuserinfo="getuserinfo">
    {{isLogin}}
  </button>
</view>